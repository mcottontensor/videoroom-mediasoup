A simple video room test utilizing mediasoup.

See server/ssl/readme.txt for ssl information
Run `npm install`
Rename `config.js.default` to `config.js`
Make any changes to `config.js`
Run `npm start`
